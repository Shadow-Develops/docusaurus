---
title: Legacy Products
description: Here you can find any product that has been marked as legacy or deprecated.
---

import Link from '@docusaurus/Link';
export const legacyCardsData = [
	{
		title: 'Community Template (Original)',
		docUrl: '/legacy/communityTemplate',
	},
];

export function LegacyCard({ icon, title, description, docUrl }) {
	return (
		<article className='col col--6 margin-bottom--lg'>
			<Link className='card padding--lg cardContainer_fWXF' to={docUrl}>
				<h2 className='text--truncate cardTitle_rnsV' title={title}>
					{title}
				</h2>
			</Link>
		</article>
	);
}

# Legacy Products

Here you can find any product that has been marked as legacy or deprecated.

<div className='row'>
	{legacyCardsData.map((props, idx) => (
		<LegacyCard key={idx} {...props} />
	))}
</div>
